# Package name is set per-lane in the Fastfile (dev/staging/prod each have
# a different applicationId), so nothing fixed here.
json_key_file(ENV["PLAY_STORE_JSON_KEY_FILEPATH"]) if ENV["PLAY_STORE_JSON_KEY_FILEPATH"]
